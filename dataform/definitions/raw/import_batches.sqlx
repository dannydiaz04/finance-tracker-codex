config {
  type: "operations",
  schema: "raw_finance",
  name: "import_batches",
  description: "Metadata for inbound CSV and Plaid import batches.",
  hasOutput: true
}

create table if not exists ${self()} (
  import_batch_id string,
  source_name string,
  imported_at timestamp,
  row_count int64,
  status string,
  file_name string,
  mapping_profile_id string,
  mapping_resolution_strategy string,
  mapping_matched_by array<string>,
  runtime_source_account_id string,
  runtime_account_name string,
  runtime_account_mask string
)
partition by date(imported_at)
options (
  description = "Metadata for inbound CSV and Plaid import batches."
)
